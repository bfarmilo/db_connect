{
  "name": "dbviewer",
  "version": "0.1.0",
  "description": "Viewer application that connects to patent DB",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . --platform=win32 --arch=x64 --version=1.3.3 --overwrite"
  },
  "author": "Bill Farmilo",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.2",
    "msnodesqlv8": "^0.1.45"
  },
  "devDependencies": {
    "shell": "^0.3.2"
  }
}
