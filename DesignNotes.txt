// Core user stories:

// 1: A user can select an output view from a predetermined list of views, select a column to search on and a value to search, and the applicatoin 


// Architecture

//module to pass in a SQL SELECT, SQL WHERE, returns (err, data) where data is an Array of the table data
//module to parse the ClaimHTML
//module to output the data array into an HTML table
//module to capture the input from a web front-end: select from a predetermined list of SELECTS, allow user to type in a keyword or match phrase and select a column to match, generate a WHERE clause

//future:

//module to add a new patent
//